<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.N2APP_EX3_" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/gold</item>
    </style>

    <style name="Theme.N2APP_EX3_" parent="Base.Theme.N2APP_EX3_" />

    <!-- Theme for pickers -->
    <style name="AppTheme.Picker" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:colorAccent">@color/gold</item>
    </style>

    <!-- Theme Overlays for specific components -->
    <style name="CalendarTheme" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorPrimary">@color/gold</item>
    </style>

    <style name="TimePickerTheme" parent="ThemeOverlay.MaterialComponents.TimePicker">
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorPrimary">@color/gold</item>
    </style>

</resources>